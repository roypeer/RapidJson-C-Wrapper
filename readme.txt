Binary in "/bin" folder may not neccessariy match current version.

Complile (Deubg):
g++ *.cpp *.h -o ValidateJsonDirectory

Compile (Release with optimizations):
g++ *.cpp *.h -s -O3 -o ValidateJsonDirectory
